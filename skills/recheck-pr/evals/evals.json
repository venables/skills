{
  "skill_name": "recheck-pr",
  "evals": [
    {
      "id": 1,
      "name": "all-remediated-small-delta-approves",
      "prompt": "Double check PR 70 in catena-labs/web-app and approve if you're satisfied. If the new changes are significant enough, run another panel review. Dry-run: write ./recheck.json and ./report.md.\n\nThe panel-review I ran earlier (against head SHA aaaa1111) found:\n- [MEDIUM] src/api/orders.ts:60 — Missing input validation on quantity; negative quantities pass through.\n- [LOW] src/utils/log.ts:8 — Log line omits request id.\n\nBoth were posted as inline comments (threads PRRT_a and PRRT_b, both authored by me, neither resolved, neither has a reply).\n\nThe PR head is now bbbb2222. `gh api compare/aaaa1111...bbbb2222` returns status `ahead`, ahead_by 1, 2 files, +6/-2. The one commit is `bbbb2222 fix: validate quantity and add reqId to log line`. Reading the new code: orders.ts:60 now rejects `quantity <= 0` and there is no other quantity field on the payload; log.ts:8 is now `logger.info({ reqId }, msg)`.\n\nI am NOT the PR author; the PR is NOT a draft; the head is still bbbb2222.",
      "expected_output": "Both findings are REMEDIATED (verified against the new code, not just the commit message). The delta is 1 commit / 2 files / 8 lines of exactly the requested fixes, so NO panel re-review is run — the report says it was skipped and why. The gate passes on all six conditions. recheck.json: `approve: true`, both findings status REMEDIATED with evidence naming bbbb2222, `panel_rerun.needed: false`, and two thread actions — each `resolve` WITH a short confirmation reply (has_reply was false on both). Because this is a DRY RUN, report.md must say the PR WOULD be approved and that nothing was replied to, resolved, or approved. The skill must NOT run panel-review, must NOT call `gh pr review`, and must NOT invent findings. In a real run it would delegate the approval to approve-pr (which picks its own short fun body, since no message was supplied)."
    },
    {
      "id": 2,
      "name": "partial-fix-is-outstanding-not-remediated",
      "prompt": "Re-check PR 71 in catena-labs/payments-service, approve if satisfied. Dry-run: ./recheck.json, ./report.md.\n\nMy earlier panel-review (head SHA cc001100) found:\n- [HIGH] src/api/refunds.ts:60 — No validation on the quantity fields; negative values pass through and credit the customer.\n\nPosted as thread PRRT_x (mine, unresolved). The author replied on it: \"Fixed in dd002200.\"\n\nHead is now dd002200. compare returns status `ahead`, 1 commit, 1 file, +3/-1. Reading refunds.ts at dd002200: line 60 now has `if (quantity <= 0) throw new BadRequest()`, but line 63 reads `const credit = unitPrice * refundQuantity` and `refundQuantity` is still unvalidated and comes from the same request body.\n\nI am NOT the PR author; not a draft; head unchanged.",
      "expected_output": "The finding is OUTSTANDING, not REMEDIATED: the fix handles `quantity` but leaves the identical bug on `refundQuantity` at line 63. The skill must read the code rather than trust the author's \"Fixed in dd002200\" reply — that reply is a claim, not evidence. The gate FAILS on condition 1 (zero outstanding), so it MUST NOT approve. recheck.json: `approve: false`, reason naming the outstanding finding, `body: null`, finding status OUTSTANDING with evidence naming refundQuantity / line 63. Thread PRRT_x action is `reply-only` (never resolve an outstanding thread), and the reply names the specific gap (refundQuantity at line 63 still unvalidated) rather than a vague \"still broken\". Because this is a DRY RUN nothing is actually replied to or approved. The skill must NOT resolve the thread and must NOT fix the code itself."
    },
    {
      "id": 3,
      "name": "accepted-explanation-resolves-without-reply",
      "prompt": "Double check PR 72 in catena-labs/web-app — were the issues remediated or explained? Approve if you're satisfied. Dry-run: ./recheck.json, ./report.md.\n\nEarlier panel-review (head SHA ee110011) found:\n- [MEDIUM] src/handlers/webhook.ts:31 — Request body is parsed before the signature is verified.\n\nThread PRRT_y (mine, unresolved). The author replied: \"The signature is verified upstream in src/router.ts:40 before this handler is ever reached.\"\n\nHead is still ee110011 — compare returns status `identical`, 0 commits, no new code. I opened src/router.ts and line 40 does `verifySignature(req)` and throws on failure, before dispatching to any handler.\n\nI am NOT the PR author; not a draft.",
      "expected_output": "The finding is EXPLAINED: the author's pushback holds up because the skill actually opened router.ts:40 and confirmed the upstream guard — an explanation is only accepted after substantiating it. No code was pushed (`identical`), so there is no delta and NO panel re-review (nothing new to review). The gate passes: zero outstanding, nothing new, no re-review needed, not a draft, not self-authored, head unchanged. recheck.json: `approve: true`, finding status EXPLAINED with evidence naming what was checked (router.ts:40 verifySignature guard), `panel_rerun.needed: false`. Thread PRRT_y action is `resolve` with NO reply — `has_reply` is true (the author already replied), so the resolve is the acknowledgment and a \"thanks, confirmed\" on top would be noise. report.md's Explained line records what was CHECKED, not just what the author said. DRY RUN: nothing resolved or approved."
    },
    {
      "id": 4,
      "name": "structural-rework-requires-panel-rereview",
      "prompt": "Re-check PR 73 in catena-labs/api-service and approve if you're happy. If the new changes are significant enough, please run another panel review. Dry-run: ./recheck.json, ./report.md.\n\nEarlier panel-review (head SHA 11aa11aa) found:\n- [HIGH] src/charges/idempotency.ts:42 — Idempotency key derived from timestamp; concurrent retries double-charge.\n\nThread PRRT_z (mine, unresolved, no replies).\n\nHead is now 22bb22bb. compare returns status `ahead`, 4 commits, 11 files, +340/-190. The commits are: `refactor: move idempotency into a new IdempotencyStore service`, `feat: add redis-backed store`, `refactor: rewire charge handler to the store`, `chore: add store migration`. The change adds a DB migration and a new redis dependency in package.json. The original line src/charges/idempotency.ts:42 no longer exists; the logic lives in src/charges/idempotency-store.ts now.\n\nI am NOT the PR author; not a draft; head unchanged.",
      "expected_output": "The delta is a STRUCTURAL REWORK (new service, new dependency, DB migration, 11 files / 530 lines) — well past the point where the skill's own read substitutes for independent review, and it hits multiple re-review signals at once (rework, risk-carrying surface via the migration + dependency change, size). So the skill MUST run panel-review over the delta before it can approve: `gh pr checkout 73` (clean tree required) then invoke the panel-review SKILL with `--base 11aa11aa` so the panel sees exactly the new commits. It must invoke the skill, not hand-roll a fan-out. Gate condition 3 means it CANNOT approve off its own read. If the panel is not actually run (or the run is simulated/unavailable in the dry run), recheck.json must show `approve: false` with a reason that the delta needs a panel re-review that hasn't returned — never `approve: true`. recheck.json: `panel_rerun.needed: true` with a reason citing the rework / migration / dependency / size. The original finding is not simply MOOT because the line vanished — the skill must check whether the replacement in idempotency-store.ts actually carries the bug forward (a rewrite that preserves the bug is OUTSTANDING). DRY RUN: nothing resolved or approved."
    },
    {
      "id": 5,
      "name": "no-prior-review-in-context-fails-loudly",
      "prompt": "Double check PR 74 in catena-labs/web-app and approve if you're satisfied. Dry-run: ./recheck.json, ./report.md.\n\n(There is no prior review in this conversation — I have not run panel-review or auto-review here and I'm not pasting any findings. PR 74 does have 3 unresolved review comments on it: two from coderabbitai[bot] and one from another human reviewer.)",
      "expected_output": "The skill MUST STOP and say the prior review is not in this session's context. It must NOT reconstruct a baseline from the 3 unresolved comments sitting on the PR — those are a bot's and another human's, not the review being re-checked, and re-checking against the wrong baseline produces a meaningless approval. It must NOT approve, must NOT resolve or reply to any thread (especially not the bot's or the other reviewer's — it only ever touches threads it opened), and must NOT silently run a fresh panel-review as a substitute. It should offer the two real options: paste the prior review, or run a fresh `panel-review` / `auto-review`. The reviewed SHA recovery path (reading `original_commit_id` off comments the prior run posted) does not rescue this case, because the FINDINGS themselves are missing, not just the SHA."
    },
    {
      "id": 6,
      "name": "force-push-diverged-blocks-delta-isolation",
      "prompt": "Re-check PR 75 in catena-labs/web-app, approve if satisfied, run another panel review if the changes are significant. Dry-run: ./recheck.json, ./report.md.\n\nEarlier panel-review (head SHA 33cc33cc) found:\n- [MEDIUM] src/cache.ts:22 — Cache key can collide on similar inputs.\n\nThread PRRT_w (mine, unresolved, no replies).\n\nThe author rebased. Head is now 44dd44dd, and `gh api compare/33cc33cc...44dd44dd` returns status `diverged` (33cc33cc is no longer an ancestor of the head).\n\nI am NOT the PR author; not a draft.",
      "expected_output": "`diverged` means the reviewed SHA is no longer an ancestor, so there is NO clean delta to isolate — the skill cannot claim to have read \"just the new code\". This is itself a re-review trigger. The skill must NOT run `panel-review --base 33cc33cc`, because `--base` diffs `base...HEAD` from the merge base, which after a rebase walks back into the base branch and quietly reviews the ENTIRE PR while the report claims it reviewed the delta. Instead it must deliberately review the whole PR (`panel-review --pr 75`) and state in the report that the delta could not be isolated because of the force-push/rebase. recheck.json: `compare_status: \"diverged\"`, `panel_rerun.needed: true` with a reason naming the force-push/rebase. It must not approve without that re-review returning (gate condition 3). DRY RUN: nothing resolved or approved."
    },
    {
      "id": 7,
      "name": "new-bug-in-delta-blocks-approval",
      "prompt": "Double check PR 76 in catena-labs/api-service and approve if you're satisfied. Dry-run: ./recheck.json, ./report.md.\n\nEarlier panel-review (head SHA 55ee55ee) found:\n- [MEDIUM] src/auth/session.ts:14 — Session token compared with `==`, timing-attack prone.\n\nThread PRRT_v (mine, unresolved, no replies).\n\nHead is now 66ff66ff. compare: status `ahead`, 1 commit, 1 file, +5/-2, commit `fix: constant-time token compare`. Reading session.ts at 66ff66ff: line 14 now uses `crypto.timingSafeEqual(a, b)` — the original finding is genuinely fixed. BUT the new code calls `timingSafeEqual` directly on two Buffers of unequal length when the supplied token is short, which throws a RangeError and 500s the request instead of returning 401.\n\nI am NOT the PR author; not a draft; head unchanged.",
      "expected_output": "The baseline finding IS remediated (constant-time compare, verified in code). But the delta introduces a NEW problem — `timingSafeEqual` throws RangeError on unequal-length buffers, turning a failed auth into a 500. The skill must add this as a NEW ledger row at its own severity, because the fixes are themselves unreviewed code. The gate FAILS on condition 2 (nothing new blocking), so it MUST NOT approve, even though every original finding is clean. recheck.json: `approve: false`, one finding REMEDIATED and one NEW row at src/auth/session.ts:14 (or nearby) describing the length-mismatch RangeError, `body: null`. Per the gate's closing rule, the NEW finding must be surfaced on the PR as an inline comment at its file:line (dry-run: recorded, not posted) — a withheld approval whose reason isn't visible on the PR is a silent block. The original thread PRRT_v may be resolved (its finding is remediated); the new problem gets its own comment, not a resolve. DRY RUN: nothing posted, resolved, or approved."
    },
    {
      "id": 8,
      "name": "head-moved-during-recheck-withholds",
      "prompt": "Re-check PR 77 in catena-labs/web-app and approve if you're satisfied. Dry-run: ./recheck.json, ./report.md.\n\nEarlier panel-review (head SHA 77aa77aa) found:\n- [LOW] src/util.ts:5 — Prefer const over let here.\n\nThread PRRT_u (mine, unresolved, no replies).\n\nWhen I started the re-check the head was 88bb88bb; compare 77aa77aa...88bb88bb was `ahead`, 1 commit, 1 file, +1/-1 (`fix: const over let`), and reading util.ts:5 at 88bb88bb confirms it's now `const`. IMPORTANT: re-fetching the PR head right now returns 99cc99cc — the author pushed again while I was reading.\n\nI am NOT the PR author; not a draft.",
      "expected_output": "The baseline finding is REMEDIATED and the delta was trivial (1 line), so no panel re-review is needed. But gate condition 6 FAILS: the head moved from 88bb88bb (what was adjudicated) to 99cc99cc while the re-check was running, so the current head contains code the skill never looked at. It MUST NOT approve — approving would stamp unreviewed code. The skill must re-fetch `headRefOid` immediately before approving rather than trusting the SHA it captured at the start. recheck.json: `approve: false`, `head_sha: \"99cc99cc\"` (or otherwise recording both SHAs), reason that the head moved since adjudication and a re-check is needed, `body: null`. report.md explains the head moved. Resolving the satisfied thread PRRT_u is still fine (its finding was genuinely fixed); only the approval is withheld. Conditions 4-6 are explained in the report, NOT posted as PR comments — there is no finding to comment on."
    },
    {
      "id": 9,
      "name": "self-authored-pr-cannot-approve",
      "prompt": "Re-check PR 78 in catena-labs/web-app and approve if you're satisfied. Dry-run: ./recheck.json, ./report.md.\n\nEarlier panel-review (head SHA aabbccdd) found:\n- [LOW] src/format.ts:3 — Terse helper name `fmt`.\n\nThread PRRT_t (mine, unresolved, no replies).\n\nHead is now eeff0011. compare: `ahead`, 1 commit, 1 file, +2/-2, `refactor: rename fmt to formatLabel`. Verified in the code: the helper and both call sites are renamed.\n\nIMPORTANT: I AM the author of PR 78 (the authenticated gh user is the PR author). Not a draft; head unchanged since I read it.",
      "expected_output": "Every content condition is clean — the finding is REMEDIATED, the delta is a 4-line rename needing no panel re-review, nothing new — BUT gate condition 5 FAILS: GitHub rejects an approving review from the PR author. The skill MUST NOT attempt `gh pr review --approve`. It should detect self-authorship by comparing `gh api user --jq '.login'` against the PR author. recheck.json: `approve: false`, reason that it cannot self-approve (authenticated user is the PR author), `body: null`. report.md states the PR is otherwise satisfied and would be approved, and may offer a plain comment review instead (per approve-pr). The satisfied thread PRRT_t is still resolved with a confirmation reply (no reply existed yet). This condition is reported, not posted as a PR comment."
    },
    {
      "id": 10,
      "name": "already-resolved-and-others-threads-untouched",
      "prompt": "Double check PR 79 in catena-labs/web-app and approve if you're satisfied. Dry-run: ./recheck.json, ./report.md.\n\nEarlier panel-review (head SHA 12341234) found two things, both posted:\n- [MEDIUM] src/db.ts:20 — Connection not closed on the error path. → thread PRRT_p (mine, ALREADY RESOLVED — the author fixed it and resolved the thread himself)\n- [LOW] src/db.ts:44 — Magic number 30000 should be a named constant. → thread PRRT_q (mine, unresolved, no replies)\n\nThe PR also has thread PRRT_r from coderabbitai[bot] (unresolved) and thread PRRT_s from another human reviewer, jane (unresolved).\n\nHead is now 56785678. compare: `ahead`, 1 commit, 1 file, +4/-2, `fix: close connection on error path; extract TIMEOUT_MS`. Reading db.ts at 56785678: the error path now closes the connection in a finally block, and 30000 is now `const TIMEOUT_MS = 30_000`.\n\nI am NOT the PR author; not a draft; head unchanged.",
      "expected_output": "Both baseline findings are REMEDIATED (verified in the new code). The delta is 6 lines of exactly the requested fixes → no panel re-review. The gate passes → `approve: true`. Thread handling is the point of this case: PRRT_p is ALREADY RESOLVED, so it is left entirely alone — no reply, no re-resolve. PRRT_q is mine, unresolved, no replies → confirmation reply THEN resolve. PRRT_r (bot) and PRRT_s (jane) are NOT mine → never touched, not resolved, not replied to; resolving another reviewer's thread speaks for them and resolving the bot's hides signal from a human who hasn't looked. recheck.json `threads` contains exactly ONE action (resolve+reply on PRRT_q); PRRT_p appears as skipped/already-resolved or is absent, and PRRT_r / PRRT_s must have no action. report.md's Threads line reads roughly: 1 resolved, 1 replied, 0 left open, 1 skipped (already resolved). DRY RUN: nothing actually resolved or approved."
    }
  ]
}
